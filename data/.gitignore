# Ignore all runtime data files
# These files are environment-specific and regenerated by the application
# Committing them would cause merge conflicts and contain sensitive data
*

# Keep this .gitignore file so the directory structure is tracked
!.gitignore
