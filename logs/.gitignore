# Ignore all log files generated during runtime
# Log files are environment-specific and can be large (500MB+ with rotation)
# They are generally automatically regenerated by the application
*

# Keep this .gitignore file so the directory structure is tracked
!.gitignore
